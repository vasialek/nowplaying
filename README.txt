Edit stations.xml file (it should be in the same directory as executable) to set which radio stations will be active (fetching information about songs)
Run application, in case of errors check logs in Logs directory (near executable file)

